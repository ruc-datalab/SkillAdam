Use exactly one outcome label:

- `outcome=success`: every required hard condition passed.
- `outcome=partial`: the hard result failed but the soft score is positive.
- `outcome=failure`: the hard result and meaningful partial progress are zero.

Include the slice, hard score, soft score, and the most relevant domain
diagnostics after the label.
