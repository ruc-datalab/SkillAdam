Below is a demonstration of the expected output format. The domain content
is illustrative only — do not transfer any of it into your output.

Input: 5 trajectory summaries from a visual document QA task.

Output:

```json
{
  "metadata_json": {
    "name": "doc-image-qa",
    "description": "Answer questions about a single document image by inspecting the image, locating the relevant region, and formatting the answer to match the question style.",
    "when_to_use": [
      "Questions ask about a single document image (form, table, figure, free text, handwritten note) and expect a short factual answer."
    ]
  },
  "skill_body_md": "# Visual Document QA\n\n## Workflow\n\n1. **Read the question first to settle question type and answer shape**: Before scanning the image, classify the question as label-to-value lookup (\"what is the value of X\"), numeric span (\"which year/date/page\"), binary (\"yes/no\"), enumeration (\"list all\"), or computation (\"how many\", \"what is the sum\"). Note whether the answer is expected to be a short proper noun, a number, a date, a phrase from the document, or a fixed yes/no token. The expected shape constrains where to look and how to format the output.\n\n2. **Identify the document layout before locating the answer**: Glance at the whole image first to recognize the layout type — form (label/value pairs in a vertical or grid arrangement), table (rows and columns with multi-row headers possible), figure or chart (axis labels, legend, caption, plotted marks), free text paragraph (continuous prose), handwritten content (annotations, signatures, filled-in form fields), or a mix. The layout type determines the navigation strategy: forms map labels to nearby values; tables require row-and-column alignment with the exact header path; figures require reading axis or legend before value marks; free text requires locating the matching phrase.\n\n3. **Locate the smallest sufficient region for the answer**: Scan only the region that the question's referent points to. For form questions, find the label that matches the question subject and read the immediately adjacent value, watching for similar-looking neighboring labels. For table questions, identify the requested row and column header path, then read the cell at their intersection — do not take values from adjacent rows or columns. For figure questions, read the axis label and legend first, then the plotted value at the requested coordinate. For free-text questions, locate the sentence or clause that contains the question's keywords and extract the answer span from it.\n\n4. **Format the answer to match the question style**: Take the answer span verbatim from the document when possible. Match the question's level of detail: bare number when asked \"how many\" or \"what value\"; number plus unit only if the question names the unit or the document writes it as one phrase; date in the form the document uses unless the question specifies otherwise; named-entity capitalization as printed; yes/no as a single word. Emit the final answer inside `<answer>...</answer>` and keep the span short — no prose, no rephrasing, no extra punctuation unless it appears in the printed text.\n\n## Error Avoidance\n\n- Do not conflate adjacent labels or columns: forms often place similar fields side by side (e.g., \"Date\" vs \"Date Filed\", \"Amount\" vs \"Total\") and tables often place similar columns next to each other (e.g., gross vs net, year-to-date vs current period). Verify the label or header path matches the exact subject of the question before recording the value.\n\n- Do not normalize or paraphrase values that should stay as printed: keep the original capitalization of named entities, the original date format the document uses, and the original spelling — the evaluator does not rewrite proper nouns to a canonical form.\n\n- Do not append units, currency symbols, or extra words unless the question explicitly asks for them or the document writes the answer as one phrase. \"How many people\" expects a bare integer, not \"5 people\"; \"what amount\" usually expects the value alone unless the unit is part of the printed answer.\n\n- Do not invent content from partial visibility: when a value is occluded, smudged, or handwritten ambiguously, prefer the most-supported visible span over a confident guess. If the question targets a region that is not visible in the image, choose the closest supported answer rather than fabricating.\n\n- Do not omit the `<answer>` wrapper: the evaluator extracts the final answer from the last `<answer>...</answer>` pair. Reasoning text outside that wrapper is ignored, but a missing wrapper means the answer falls back to the last non-empty line of the response, which is often the wrong span."
}
```
