The domain and content below are illustrative only — do not transfer any
of it into your output.

```json
{
  "reasoning": "## Observations\n\n[1] (failure, hard=0, per_cell=0.78): The agent wrote a sorting script that used `ws.append()` to rebuild the output sheet. Since the destination sheet already had content, intermediate rows from a previous attempt were never cleared, leaving stale data mixed with new output.\n\n[3] (failure, hard=0, per_cell=0.00, exec=0): The agent tried `ws['A'] = values` expecting column-level assignment, which is not valid openpyxl syntax. The script raised AttributeError and never produced output.\n\n[2] (success, hard=1): The agent read all data with `iter_rows(min_row=2, values_only=True)`, built a list of tuples, sorted by the requested column, then wrote each row back with explicit `ws.cell(row=r, column=c).value = val`.\n\n## Patterns\n\nBoth failures involve using shorthand operations (append, column assignment) where explicit cell-by-cell writes would have been safer. [2] demonstrates explicit writes working correctly. The current skill says 'apply smallest edit' but does not mention the append-vs-explicit-write distinction, which caused the failure in [1].\n\n## Proposed Changes\n\n1. **Add** Code Pattern: explicit cell writes for known target ranges (evidence: [1] used append and left stale data; [2] used cell-by-cell and succeeded).\n2. **Add** Error Avoidance rule noting that `ws.append()` writes after the last used row, not at a caller-specified position.\n3. **Revise** Workflow step 3 to mention clearing the target range before writing when overwriting existing content.",
  "patch": "--- a/SKILL.md\n+++ b/SKILL.md\n@@ -8,7 +8,7 @@\n 1. Inspect the workbook preview ...\n 2. Read representative rows ...\n-3. Apply the smallest necessary edit to the live workbook, preserving unrelated sheets, formulas, and formatting. Candidate tools: `openpyxl` cell assignment, `insert_rows`, `delete_rows`, `move_range`.\n+3. When overwriting existing content, consider clearing the target range first (delete rows or set cells to None), then write each output cell with `ws.cell(row=r, column=c).value = val`. This can prevent stale data from previous attempts contaminating the output.\n 4. When the task asks for a lookup ...\n@@ -14,4 +14,10 @@\n ## Error Avoidance\n ...\n - Do not assume one matching rule fits every workbook; confirm it against sample rows first.\n+- `ws.append()` writes after the last used row, not at a caller-specified position — use explicit cell assignment when targeting a known range.\n+\n+## Code Patterns\n+- **Explicit cell write** (when the output range is known):\n+  `ws.cell(row=start_row+i, column=col_idx).value = val`\n+  Avoids stale-data contamination from append-based writes.\n"
}
```
